export type SocialButton = {
    name: string;
    color: string;
    link: string;
    className: string;
};

export type LinkItemsProps = {
    name: string;
    route: string;
    enName: string;
}